# x402r Contracts Deployment Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# Required for Forge Deployment
# =============================================================================

# Private key of the deployer account (must have funds for gas)
PRIVATE_KEY=

# Etherscan API key for contract verification (global in Foundry v2)
ETHERSCAN_API_KEY=

# =============================================================================
# Required for DeployAll.s.sol (Master Deployment Script)
# Note: This script deploys only factories. Factory instances are deployed on-demand via SDK.
# =============================================================================

# Address to receive protocol fees (immutable - cannot be changed after deployment)
# Can be the same as OWNER_ADDRESS if desired
PROTOCOL_FEE_RECIPIENT=

# Owner of the operator/factory contracts (can configure fees via setFeesEnabled())
# Controls administrative functions: setFeesEnabled() and rescueETH()
# Can be the same as PROTOCOL_FEE_RECIPIENT if desired
# Note: Owner can be transferred, but PROTOCOL_FEE_RECIPIENT is immutable
OWNER_ADDRESS=

# =============================================================================
# Optional for DeployAll.s.sol (with defaults)
# =============================================================================

# Maximum total fee rate in basis points (default: 5 = 0.05%)
MAX_TOTAL_FEE_RATE=5

# Protocol fee percentage 0-100 (default: 25 = 25% to protocol, 75% to arbiter)

# Note: PayerFreezePolicy is automatically deployed by DeployAll.s.sol.
# Its address will be output in the deployment summary for use when creating condition instances.

PROTOCOL_FEE_PERCENTAGE=25
# =============================================================================
# Required for Individual Deployment Scripts
# =============================================================================

# For DeployArbitrationOperator.s.sol (if deploying individually)
ESCROW_ADDRESS=
RELEASE_CONDITION=

# For DeployEscrowPeriodCondition.s.sol (if deploying individually)
# Note: This script deploys only the factory, not instances.

# =============================================================================
# RPC URLs (for forge script --rpc-url)
# =============================================================================

# Base Mainnet
# BASE_MAINNET_RPC=https://mainnet.base.org

# Base Sepolia
# BASE_SEPOLIA_RPC=https://sepolia.base.org

# Optimism Mainnet
# OPTIMISM_RPC=https://mainnet.optimism.io

# Arbitrum One
# ARBITRUM_RPC=https://arb1.arbitrum.io/rpc
